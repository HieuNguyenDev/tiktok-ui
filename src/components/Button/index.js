import myAlert from '~/myAlert';

function Button() {
    return (
        <div>
            <div>
                <div></div>
            </div>
            <button onClick={myAlert}>Click me!</button>
        </div>
    );
}

export default Button;
